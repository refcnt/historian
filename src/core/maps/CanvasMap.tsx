/**
 * Placeholder for future canvas/SVG-based map (architecture diagrams, network topology).
 * Will render CanvasLocation-based nodes as positioned elements on a 2D canvas.
 */
export default function CanvasMap() {
  return (
    <div style={{ width: '100%', height: '100%', background: '#0a0e1a',
                  display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#555' }}>
      Canvas map — not yet implemented
    </div>
  )
}
